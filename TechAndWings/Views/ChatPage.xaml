<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" 
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
    xmlns:local="clr-namespace:TechAndWings;assembly:TechAndWings"
    x:Class="TechAndWings.Views.ChatPage">
	<ContentPage.Content>
        <local:ChatListView ItemsSource="{Binding Messages}">
            <local:ChatListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <StackLayout Padding="10">
<!--                                <Label Text="{Binding Text}" HorizontalOptions="{Binding Position}" BackgroundColor="Aqua" TextColor="White" Style="{DynamicResource ListItemTextStyle}" FontSize="16" />-->
                            <local:MessageBubble Message="{Binding Text}" MyMessage="{Binding MyMessage}"/>
                            </StackLayout>
                        </ViewCell>
                    </DataTemplate>
                </local:ChatListView.ItemTemplate>
        </local:ChatListView>
	</ContentPage.Content>
</ContentPage>
